- if current_member
	#member
		.name.text_center
			= current_member.name
- else
	#login
		- Authorization::PROVIDERS.each do |x|
			= link_to image_tag("icon/#{x}_big.png",:title => x),member_omniauth_authorize_path(x.to_sym)
